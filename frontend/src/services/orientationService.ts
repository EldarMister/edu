/**
 * OrientationService — REMOVED.
 *
 * Ориентацией экрана теперь управляет система телефона и браузер.
 * Ручная блокировка (screen.orientation.lock/unlock) удалена,
 * т.к. она конфликтовала с системным автоповоротом.
 *
 * Для адаптации UI используйте CSS:
 *   @media (orientation: portrait) { ... }
 *   @media (orientation: landscape) { ... }
 *
 * Этот файл оставлен как заглушка для документации.
 * Если ни один модуль его не импортирует — можно безопасно удалить.
 */
