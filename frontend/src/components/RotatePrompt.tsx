/**
 * RotatePrompt — REMOVED.
 *
 * Оверлей «Поверните устройство» больше не нужен.
 * Ориентацией управляет система телефона, а UI адаптируется через CSS media queries.
 *
 * Этот файл оставлен как заглушка. Если ни один модуль его не импортирует — удалите.
 */
